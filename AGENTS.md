# LeetCode 刷题项目 - AI 辅助约定

本项目为 LeetCode 刷题仓库，按 [Top 100 Liked](https://leetcode.cn/studyplan/top-100-liked/) 计划准备面试。AI 作为刷题辅助员，在用户提出无思路或思路一般的题目时，提供题解与代码。

## 技术栈

- Go 1.20+
- 代码与文档均使用 UTF-8 编码

## 文件结构

```
leetcode/
├── codes/          # 题解代码（Go）
├── docs/           # 题解文档（Markdown）
└── AGENTS.md       # 本文件
```

## 命名规范

统一格式：`{LeetCode 编号}-{英文题名}`

- 代码：`codes/0049-group-anagrams.go`
- 文档：`docs/0049-group-anagrams.md`

编号不足 4 位时前补 0（如 1 → 0001）。

## 代码约定

- **保持 LeetCode 原函数签名**，便于用户复制到 LeetCode 提交
- 只保留**一个**解法：选最易理解或最优解，不罗列多种

## 文档约定

- 开头先放**一分钟复习**，再包含完整**题目**：题干、输入输出、示例；兼顾快速回忆与脱离原题复习
- 内容按训练链组织：**识别题型 → 推导解法 → 明确不变量 → 模拟执行 → 主动回忆 → 迁移到同类题**
- 文档内需包含跳转到对应 Go 文件的链接，例如：
  ```markdown
  **代码**：[codes/0049-group-anagrams.go](../codes/0049-group-anagrams.go)
  ```

### 题解叙述风格（重要）

在**简洁**的前提下，兼顾**可理解**与**可迁移**：

1. **知识点首次出现要「点一句」**  
   若正文里第一次用到**双端队列、前缀和、滑动窗口、单调栈/单调队列、哈希计数**等，用 **2～4 句话**说明：它是什么、在本题里扮演什么角色。不要求讲透体系，避免默认读者已经会。

2. **先写「怎么想到」，再写「怎么做」**  
   用一小节（如 **「怎么想到」** 或 **「思考路径」**）写清决策链：**题目在问什么 → 朴素做法是什么 → 瓶颈或重复计算在哪里 → 哪些信息值得保留 → 为什么当前数据结构或技巧能维护这些信息**。避免一上来只列步骤、不交代动机。

3. **控制总长**  
   整篇思路部分以**一到两屏阅读**为宜；多用**短段落、列表**，少写冗长散文。优先保留**决策链、核心不变量、关键状态变化、主动回忆问题**，删除重复说明和次要细节。

4. **解释为什么正确，而不只描述步骤**
   单独写出算法的**核心不变量**，说明执行过程中始终成立的条件。正确性解释只聚焦非显然操作，例如为什么某个候选可以永久删除、为什么移动某个指针不会漏解、为什么状态转移覆盖所有情况。

5. **用主动回忆代替被动重读**
   文末给出 **3～5 个自测问题**，覆盖关键选择、不变量、复杂度和一种简单变形；问题后不直接附答案，答案应能从正文推导。

### 分层训练型题解模板

题解默认按以下顺序组织：

1. **一分钟复习**：用 4～6 条短句写识别信号、核心方法、核心不变量、关键操作和复杂度。
2. **题目**：保留题干、输入输出、示例及必要约束。
3. **怎么想到**：按“朴素方法 → 瓶颈 → 可复用信息 → 当前解法”的决策链推导。
4. **核心不变量**：用一两句话精确定义算法始终维护的条件，以及它与答案的关系。
5. **算法步骤**：简洁列出实际执行顺序，不重复大段思路。
6. **执行过程（按需）**：当状态变化不直观时，用一个尽量小的例子或表格展示关键变化；简单题可以省略。
7. **正确性解释**：解释非显然操作为何安全、为何不会漏解；显然步骤无需逐条证明。
8. **复杂度**：给出时间、空间复杂度，并说明容易误判的摊还分析。
9. **易错点**：每项使用“**错误想法 + 为什么会错**”，避免只记录结论式口诀。
10. **自测**：设置 3～5 个不直接给答案的主动回忆问题。
11. **同类题与迁移**：列出相关题，并说明算法骨架中“保持不变”和“需要改变”的部分。

模板服务于理解和复习，不追求章节齐全带来的篇幅增长。简单题允许合并相邻章节；“执行过程”等按需章节可省略，但**一分钟复习、怎么想到、核心不变量、复杂度、易错点、自测、同类题与迁移**应尽量保留。

### 同类题与迁移一节写什么

- **必须包含同类 LeetCode 题**：列出与本题**核心套路相同或相近**的题目（带题号 + 简短题名），优先使用“题目 / 保持不变 / 需要改变”的表格，帮助识别可复用骨架与变化点。
- **可补充抽象变种**：如「改成本质不同但思路可对标的题型」，仍保持简短。
- **篇幅**：仍以表格或短列表为主，不展开成第二篇题解；没有特别合适的同类题时可不写满，不凑数。

## 行为边界

- 仅在用户明确请求某道题的题解时，才新增或修改 `codes/` 与 `docs/` 下的文件
- 不主动创建文档或代码，除非用户要求
- 回答问题时优先给出文字解释，再视需要补充代码或文档
